# DataPlat API

FastAPI 模块化单体：工作空间、数据资产、SQL Gateway、AI 问数。

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements-dev.txt
uvicorn app.main:app --reload --port 8000
```

OpenAPI：http://127.0.0.1:8000/docs
